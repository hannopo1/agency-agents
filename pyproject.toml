[project]
name = "agency-agents"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "claude-agent-sdk>=0.2.87",
    "composio>=0.13.1",
    "composio-claude-agent-sdk>=0.13.1",
]
